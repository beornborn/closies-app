//@flow

const handleClustersOverlap = (clusters: Array<Object>) => {
  const handledCLusters = clusters.map(c => {

  })
  return clusters
}

export default handleClustersOverlap

// 1. find 1 px in lat and long
// 2. coord bottom, left, top and right of cluster
// 3. if sum of abs more then center distance then
// 4. update
